id: pbk_iter28_vent_realistic
description: iter28 — vent playbook, realistic distress opener (not trigger word).
testMode: true
locale: zh-CN
agentId: default
participant: "+19999992871"
chatId: "iMessage;+19999992871"
turnTimeoutMs: 120000
turns:
  - user: "今天又被 manager 当众挂了 心慌到现在缓不过来"
    assert:
      reply_min_length: 4
      reply_not_contains_any:
        - "卧,"
        - "X 还是 Y"
