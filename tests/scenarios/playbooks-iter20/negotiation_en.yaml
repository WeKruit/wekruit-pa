id: pbk_iter20_negotiation_en
description: |
  iter 20 — verify NEGOTIATION playbook fires on direct trigger.
testMode: true
locale: en-US
agentId: default
participant: "+19999992731"
chatId: "iMessage;+19999992731"
turnTimeoutMs: 120000
turns:
  - user: "I just got 2 offers and need to negotiate. What number should I ask for?"
    assert:
      reply_min_length: 4
      reply_not_contains_any:
        - "作为 AI"
        - "As an AI"
        - "Pros:"
