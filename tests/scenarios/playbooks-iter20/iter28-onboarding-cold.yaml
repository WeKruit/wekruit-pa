id: pbk_iter28_onboarding_cold
description: iter28 — fresh user cold-start, no playbook trigger, just casual hi.
testMode: true
locale: zh-CN
agentId: default
participant: "+19999992807"
chatId: "iMessage;+19999992807"
turnTimeoutMs: 120000
turns:
  - user: "你好"
    assert:
      reply_min_length: 4
  - user: "做 SWE 的 找的湾区"
    assert:
      reply_min_length: 4
  - user: "工作 3 年 H1B"
    assert:
      reply_min_length: 4
