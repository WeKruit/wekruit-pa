# Intent Matrix — resume_parse × en × mid
# Generated 2026-05-02 by P7 bilingual intent matrix task.
# 6 intents × 3 langs × 3 personas = 54 cells total.
id: intent_matrix_resume_parse_en_mid
description: |
  Intent: resume_parse
  Language: en
  Persona: mid
    YoE: 3-5
    Status: Mid-level SWE at Series-B startup, 3-5 YoE backend/full-stack
    Visa: H1B (currently capped, employer-sponsored)
    Target: lateral move to senior SWE at bigger comp

  Validates Claire's response on this (intent × lang × persona) cell.
  Axes: language_compliance, intent_recognition, tone_friend, length_cap,
  safety (abuse_offtopic only), tag_match (job_search only).

  Run:
    PA_RUN_EVAL=1 PA_OPENAI_AGENT_API_KEY=... \
      GOOGLE_APPLICATION_CREDENTIALS=... \
      node tests/scenarios/runner.mjs tests/scenarios/intent-matrix/resume_parse_en_mid.yaml
testMode: true
locale: en-US
agentId: default
participant: "+19999991013"
chatId: "iMessage;+19999991013"
turnTimeoutMs: 120000
turns:
  - user: "pasting my resume below, take a look"
    assert:
      reply_min_length: 4
      reply_not_contains_any:
        - "好的，我记住了"
        - "已记录"
        - "作为 AI"
        - "我是 AI"
        - "I'm an AI"
        - "As an AI"
      judge:
        criterion: "The reply is predominantly English. It MUST NOT contain Chinese characters (CJK) except as proper-noun glosses. Length: ≤3 sentences. Tone: friend, not coach. NO numbered lists, NO 'N步法', NO 'try these three'. NO robot filler ('好的, 我记住了', 'It\\'s important to', 'Of course'). NO 'X 还是 Y?' clinical A/B framework questions. Intent: acknowledge resume content; if extracting fields, list YoE/skills concisely; do not invent data not in the paste."
        threshold: 0.55
  - user: "4 YoE backend.\\n- SWE II @ Stripe 2022-now (Go, AWS).\\n- SWE @ Robinhood 2020-2022.\\n- BS CS @ Berkeley 2020."
    assert:
      reply_min_length: 4
      reply_not_contains_any:
        - "好的，我记住了"
        - "已记录"
        - "作为 AI"
        - "我是 AI"
        - "I'm an AI"
        - "As an AI"
      judge:
        criterion: "The reply is predominantly English. It MUST NOT contain Chinese characters (CJK) except as proper-noun glosses. Length: ≤3 sentences. Tone: friend, not coach. NO numbered lists, NO 'N步法', NO 'try these three'. NO robot filler ('好的, 我记住了', 'It\\'s important to', 'Of course'). NO 'X 还是 Y?' clinical A/B framework questions. Intent: acknowledge resume content; if extracting fields, list YoE/skills concisely; do not invent data not in the paste."
        threshold: 0.55
  - user: "can you parse out my YoE and skills?"
    assert:
      reply_min_length: 4
      reply_not_contains_any:
        - "好的，我记住了"
        - "已记录"
        - "作为 AI"
        - "我是 AI"
        - "I'm an AI"
        - "As an AI"
      judge:
        criterion: "The reply is predominantly English. It MUST NOT contain Chinese characters (CJK) except as proper-noun glosses. Length: ≤3 sentences. Tone: friend, not coach. NO numbered lists, NO 'N步法', NO 'try these three'. NO robot filler ('好的, 我记住了', 'It\\'s important to', 'Of course'). NO 'X 还是 Y?' clinical A/B framework questions. Intent: acknowledge resume content; if extracting fields, list YoE/skills concisely; do not invent data not in the paste."
        threshold: 0.55
