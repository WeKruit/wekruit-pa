# S8 Flywheel HITL Eval Scenarios

These fixtures are declarative inputs for the S8 eval harness. They are redacted
by default and must not include raw contact details, raw prompts, raw
transcripts, or storage locators.

Expected execution command after Task B exports land:

```sh
pnpm --dir tests/eval/s8-flywheel-hitl-eval test
```
