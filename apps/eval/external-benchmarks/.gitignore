# Phase 39 — external benchmarks harness
# Vendored benchmark repos (Adam clones per SETUP.md)
vendor/

# Runtime outputs (generated by --live runs)
results/*.json

# Keep directory structure
!results/.gitkeep
