/**
 * Phase 36 — (removed) Chinese imperfection policies.
 *
 * The product is English-only (Adam 2026-05-31 P0). The Chinese policy bank
 * has been deleted: the injector must never apply Chinese slang/hesitation
 * markers. This file is intentionally empty of any policy content — the
 * injector now routes every turn through the English bank (POLICIES_EN).
 *
 * Kept as an empty module so any stale import path resolves without error;
 * no symbol is exported.
 */
export {}
