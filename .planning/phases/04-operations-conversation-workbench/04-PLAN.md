# Phase 4 Plan: Operations and conversation workbench

## Tasks

1. Add conversation search/filter and derived latest message / last error columns.
2. Expand conversation detail into linked sections: transcript, turns, outbound, connectors, audit/safety, and memory.
3. Add Operations tabs and status filters.
4. Require operator reason capture before retry/dead-letter actions.

## Verification

- `npm run typecheck --workspace=@pa/dashboard-web`
- `npm run build --workspace=@pa/dashboard-web`
