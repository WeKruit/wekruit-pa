# v1.1 implementation tranche — 2026-04-27

## Shipped in this session

### Phase 20 — Output normalizer (code-complete)

- New [`packages/pa-orchestrator/src/output-normalizer.ts`](../../packages/pa-orchestrator/src/output-normalizer.ts): `normalizeForIMessage`, UTM strip list, markdown → plain text, list markers → `· `, length cap + `planChunks` multi-bubble when >600 chars.
- New [`packages/pa-orchestrator/src/output-normalizer.test.ts`](../../packages/pa-orchestrator/src/output-normalizer.test.ts) (9+ cases including Tesla mixed case, idempotence).
- Orchestrator: multi-part `pa_outbound` when `norm.chunks.length > 1` (`outbound-${event.id}-c0`, …); transcript `appendMessage` keeps joined `norm.text`; `rawMeta.outputChunks` / `droppedUrlParams` when present.

### Phase 18 — Rollback + tests

- New [`packages/pa-orchestrator/src/legacy-voice-prompt.ts`](../../packages/pa-orchestrator/src/legacy-voice-prompt.ts) (`LEGACY_V0_SYSTEM_PROMPT`).
- New [`packages/pa-orchestrator/src/voice-reminder.test.ts`](../../packages/pa-orchestrator/src/voice-reminder.test.ts).

### P0 Safety (v1.1-EXECUTION-PLAN §7)

- `OrchestratorStore.checkInboundSafety` — default Firestore impl: `enforceRateLimit` → `checkPromptInjection` → `appendAuditEvent` on deny.
- `processInboundEvent`: after user line + reset, before memory/LLM — short-circuit with English user-facing line + turn `errorCode` set.
- Tests: mock `checkInboundSafety` deny → LLM not called.

### Dependencies

- `@pa/pa-orchestrator` now depends on `@pa/pa-broker` and `@pa/pa-safety`.

## Not yet in this tranche (follow execution plan)

- Phase 18: `voice-axes.mjs`, `pairwise.mjs`, six `eval-voice-*.yaml`, judge 5th axis `iMessage_render_safe` in [`tests/scenarios/judge.mjs`](../../tests/scenarios/judge.mjs).
- Phase 21: Sendblue `apps/functions/src/sendblue/*`, `paSendblueWebhook`, `paSendblueOutbox`, `PA_CHANNEL_LEGACY` worker exit.
- §6 LLM↔LLM multi-turn harness script.

## Verification

```bash
cd packages/pa-orchestrator && npm test
cd apps/functions && npm run typecheck
```

## Next commit message suggestion

```
feat(orchestrator): output normalizer, legacy voice, inbound safety gate

- Add normalizeForIMessage + tests; multi-chunk outbound idempotency keys
- Wire enforceRateLimit + checkPromptInjection via checkInboundSafety
- Add LEGACY_V0_SYSTEM_PROMPT module; voice-reminder tests
```
