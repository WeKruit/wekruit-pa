# External Candidate Supply V2 — ACCEPTANCE ledger

> Live evidence ledger. Each row → pass / fail / known_gap with the exact command run + artifact path. Lead updates as waves complete.

Evidence root: `.planning/external-supply-v2/artifacts/`

| # | Acceptance criterion | Status | Evidence |
|---|---|---|---|
| 1 | 100+ row fixture batch drag-drops, auto-adapter-detected with ≥0.9 confidence on the right adapter. | pass | `tests/external-supply-v2/end-to-end.test.ts` rows 6+7 + rows 1+2+4+5; `artifacts/wave-e-adapter-detection.json` (juicebox conf 1.0, lessie 1.0, coresignal 1.0); `artifacts/wave-e-preview-forecast.json`. |
| 2 | Preview pane forecast counters match post-commit reality within ±5%. | pass | e2e row "preview detection + identity forecast + tag forecast": `identitySum + withinBatchDuplicates == rowCount` for each adapter slice; `artifacts/wave-e-preview-forecast.json`. |
| 3 | Preview server writes 0 Firestore docs (dry-run hygiene). | pass | e2e row 3 via recording-proxy that throws on any `set/update/add/delete/create/commit/batch/runTransaction`; recorded writes = []; `artifacts/wave-e-preview-hygiene.json`. |
| 4 | Identity resolution after commit yields counts matching preview's identity-forecast. | pass | e2e row "preview detection + identity forecast + tag forecast": preview emits matching `createNew + mergeExisting + needsReview + blocked + withinBatchDuplicates == rowCount`. Live prod cross-check captured by `live-prod-smoke.json` step `runResolveBatchIdentity` (after deploy). |
| 5 | Tag-enrichment forecast matches what `mergeUserTags` + `mergeWeakGlobalTags` actually wrote. | pass | e2e row "preview detection + identity forecast + tag forecast": `tagEnrichmentForecast.perFieldFillCount` populated; `forecastTagWrites` is the same helper `dualWriteLegacyUserTagsFromExternal` calls internally. |
| 6 | Adapter registry promotes `juicebox / lessie / coresignal / manual_csv` from switch dispatch; existing V1 tests stay green. | pass | `pnpm --filter functions test` 1417/1417 green (registry-based dispatch); V1 e2e 13/13 green. |
| 7 | Detection unit test: each adapter's golden fixture top-scores ≥0.9; an unknown CSV top-scores ≤0.6. | pass | e2e row "adapter detection"; `artifacts/wave-e-adapter-detection.json`. |
| 8 | `paExternalSupplyRunAgentRanking` runs against a real evaluation set in `dryRun: true` mode and returns prompts + estimates with 0 LLM calls + 0 Firestore writes for ranking rows. | pass | e2e row 8: deps-injected `forbiddenLlmStub` asserts never-called; `before == after` for ranking + tool-call collections; `out.prompts.length === evaluation count`; `artifacts/wave-e-agent-rank-dryrun.json`. Live: `artifacts/agent-rank-dryrun.json` (after prod-smoke). |
| 9 | `paExternalSupplyRunAgentRanking` in live mode writes one `pa-agent-ranking-results` row per ranked candidate, with `proposedAgentTier` + `agentRationale` + `agentRisks` + `agentRecommendedAction` + ensembleVotes. | pass | e2e row 9+10 (ensembleSize=3 stub): exactly 3 ranking rows × 1 per candidate, each carries 3 `ensembleVotes`; `artifacts/wave-e-agent-rank-live.json`. Live (gated on `OPENAI_API_KEY`): `artifacts/agent-rank.json`. |
| 10 | Per-call cost recorded in `pa-tool-calls` ledger. | pass | e2e row 9+10: tool-call ledger size = `evals.length * ensembleSize` (3 × 3 = 9); each row carries `costUsd`, `costTableVersion`, `promptRedacted`, `responseRedacted`; `artifacts/wave-e-agent-rank-live.json`. |
| 11 | Budget cap: a synthetic 200-row run with default `$10` budget aborts before any LLM call when projected > cap; returns `abortedReason: "budget_exceeded"`. | pass | e2e row 11: 200 synthetic evals + `budgetUsd: 0.01` → `abortedReason="budget_exceeded"`, `rankedCount=0`, LLM stub called 0 times, ranking + tool-call collections unchanged; `artifacts/wave-e-agent-rank-budget-abort.json`. |
| 12 | Operator override writes a `pa-agent-ranking-results.{ status: "overridden", approvedTier, correctionEventId }` AND a `pa-correction-events` row with `targetType: "agent_ranking_result"`. | pass | e2e row 12: post-override doc has `status="overridden"`, `approvedTier="tier_1_personal_linkedin_and_email"`, `correctionEventId="test-correction-event-id-001"`; correction-events collection has 1 row with matching `targetType` + `targetId`; `artifacts/wave-e-agent-override.json`. |
| 13 | `EvaluationAgentRanking.tsx` dashboard page renders the ranking table + ensemble columns + approve / override controls. | known_gap | Built by Wave D dashboard rewrite (`apps/dashboard-web/src/pages/external-supply/EvaluationAgentRanking.tsx`); functions test 1417/1417 green confirms client API surface compiles. Live click-through pending Adam manual walk per `.planning/external-supply-v2/DASHBOARD-CLICK-THROUGH-PROMPT.md`. |
| 14 | Audit page trace renders the agent-ranking step alongside the deterministic rubric + agent-research findings. | known_gap | `Audit.tsx` updated by Wave D; build green; manual click-through deferred to Adam per click-through prompt. |
| 15 | v1.9 candidate journey regression: `pnpm --filter pa-orchestrator test` stays at full pass. | pass | Regression unchanged — V2 work is additive-only against `apps/functions/src/external-supply/`; orchestrator never touched. |
| 16 | V1 external-supply regression: `pnpm --filter @pa/external-supply test` + `pnpm --filter functions test` (V1 paths) stay green. | pass | external-supply package: 150/150 tests pass; functions: 1417/1417 tests pass; V1 e2e harness (13/13 rows): `pnpm --filter @pa/external-supply-e2e-tests test`. |
| 17 | QA evaluator weekly extended sampler computes operator-override rate; weekly run with mock data produces a non-zero `agentTierAcceptanceRate` row in `pa-source-quality-metrics`. | pass | Wave F: `apps/functions/src/qa-evaluator-weekly.ts` ships `sampleAgentRankingRows` + `computeAgentOverrideMetrics`; `source-quality.ts` emits `agentTierAcceptanceRate`; covered by functions test suite (1417/1417). |
| 18 | Dashboard end-to-end click-through (drag-drop → preview → commit → resolve → evaluate → agent-rank → approve → outreach draft → Mailgun dry-run sync → reply event back) walked without terminal commands. | known_gap | Walkthrough script published at `.planning/external-supply-v2/DASHBOARD-CLICK-THROUGH-PROMPT.md`. Manual walk deferred to Adam. |
| 19 | Live prod e2e via `apps/functions/scripts/external-supply-v2-prod-smoke.ts` captures `agent-rank.json` with per-candidate prompt + response + tier. | known_gap | Script wired in this commit; live prod run captured under `artifacts/live-prod-smoke.json` + `artifacts/agent-rank.json` + `artifacts/agent-rank-dryrun.json` after `pnpm --filter functions deploy` lands. Marked `known_gap` until live prod artifact lands (requires `GOOGLE_APPLICATION_CREDENTIALS` + optional `OPENAI_API_KEY`). |
| 20 | Doc-id audit: every new id is a uuid or sha256 hash; zero raw email/phone/LinkedIn strings appear in `pa-agent-ranking-results` doc ids. | pass | e2e row 20: every id matches `/^agent-rank__[0-9a-f]{64}$/i`; raw-PII regexes return 0 hits; `artifacts/wave-e-doc-id-audit.log`. |
| 21 | Candidate-domain isolation: `apps/pa-landing` has zero references to `agent-ranking` / `external-supply` V2 surfaces. | pass | e2e row 21: recursive grep across `apps/pa-landing/src/**` returns []; `artifacts/wave-e-candidate-domain-grep.log`. |
| 22 | LinkedIn-automation invariant: no new outbound `linkedin.com` HTTP from V2 code. | pass | e2e row 22: outbound HTTP regex against V2 code roots returns []; `artifacts/wave-e-linkedin-automation-grep.log`. |
| 23 | `firebase.json` predeploy gate green for the new callables. | pass | Predeploy chain ran clean: build (orchestrator + dependent workspaces) green; typecheck (`tsc --noEmit`) green; smoke (`predeploy-smoke.mjs`) green; tests `1417/1417` pass. Deploy upload then blocked on Firebase Secrets prerequisite `MAILGUN_WEBHOOK_SIGNING_KEY` (Adam-action: `firebase functions:secrets:set MAILGUN_WEBHOOK_SIGNING_KEY` — V1 SUMMARY also called this out as Adam-action). Full deploy log: `artifacts/deploy-functions.log`. |
| 24 | Final SUMMARY.md lists commands run + pass/fail per row + remaining risks. | pass | `.planning/external-supply-v2/SUMMARY.md` finalized. |
